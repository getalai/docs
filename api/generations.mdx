---
title: "Playground"
openapi: "POST /generations"
---

## Using the Playground

The request body accepts a JSON object containing all your generation options.

<Tip>
  Only `input_text` is required. All other options have sensible defaults, so
  you can start simple and add customization as needed.
</Tip>
